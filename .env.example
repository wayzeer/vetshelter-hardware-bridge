# VetShelter Hardware Bridge Configuration

# Server port (default: 3456 to match existing TPV_BRIDGE_URL)
PORT=3456

# API key for authentication (set same key in VetShelter .env as HARDWARE_BRIDGE_API_KEY)
API_KEY=vetshelter-hardware-2024

# Printer interface - Use Windows printer name
# Run GET /printers endpoint to see available printers
# Common formats:
#   printer:POS-58           (for USB printers appearing as POS-58)
#   printer:UNYKACH          (for Unykach branded printer)
#   printer:Thermal Receipt  (generic thermal)
PRINTER_INTERFACE=printer:POS-58

# For ngrok tunnel (optional - set after running ngrok)
# NGROK_URL=https://your-subdomain.ngrok.io
